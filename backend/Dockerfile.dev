# Backend開発用Dockerfile

FROM node:18-alpine

WORKDIR /app

# システム依存関係
RUN apk add --no-cache \
    curl \
    postgresql-client

# package.jsonをコピーして依存関係をインストール
COPY package*.json ./
RUN npm ci

# Wranglerをグローバルインストール
RUN npm install -g wrangler

# アプリケーションコードをコピー
COPY . .

# ポート公開
EXPOSE 3000

# 開発サーバー起動
CMD ["npm", "run", "dev"]